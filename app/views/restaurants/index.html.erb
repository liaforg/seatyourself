<h1> Seat Yourself </h1>


<% @restaurants.each do |restaurant| %>
  <% if current_user %>
    <p><%=link_to 'Edit', edit_restaurant_path(restaurant.id)%></p>
  <%end%>
  <p> <%= link_to(image_tag(restaurant.url), restaurant_path(restaurant))%> </p> <br />
  <p><%=restaurant.name%> </p>
<% end %>
