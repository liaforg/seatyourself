<h1><%= "#{@restaurant.name}" %></h1>

<%=link_to("Edit", edit_restaurant_path(@restaurant.id)) %> |
<%=link_to("Delete", restaurant_path(@restaurant.id), method: :delete, data: {confirm: "Are you sure you want to delete?"}) %>

<p>
<%= render 'single_restaurant', restaurant: @restaurant %>
</p>

<%= link_to 'Book Now', new_restaurant_reservation_path(@restaurant.id) %>
